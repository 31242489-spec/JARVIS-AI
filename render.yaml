services:
  - type: web
    name: jarvis-cloud-backend
    runtime: python
    plan: free
    rootDir: cloud_backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    healthCheckPath: /health
